<Window x:Class="tpfred2.Views.ConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configuration" Height="170" Width="520" WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="* Jeton API :" Grid.Row="0"/>

        <TextBox Grid.Row="1"
                 Text="{Binding Token, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,6,0,12"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Sauver"  Width="100" Margin="0,0,8,0" Command="{Binding SaveCommand}"/>
            <Button Content="Annuler" Width="100" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>
